export type Colors = "primary" | "secondary" | "danger" | "warning" | "default";

export const colors = {
  primary: "primary",
  secondary: "secondary",
  danger: "danger",
  warning: "warning",
  default: "default",
};
